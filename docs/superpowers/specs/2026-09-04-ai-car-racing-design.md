# AI Car Racing - спецификация

Дата: 2026-09-04
Статус: утверждено

## 1. Что это

Игра-песочница на pygame. Раунд состоит из трёх шагов:

1. Нейросеть-генератор рисует гоночную трассу.
2. Нейросеть-генератор рисует машинку и её физические параметры.
3. Нейросеть-водитель обучается эволюцией проезжать эту трассу от старта до финиша.

Раунды запускаются по кругу с новыми генерациями. Статистика копится между запусками, лучшие мозги сохраняются на диск.

## 2. Стек

Python 3.13, pygame 2.6.1, numpy 2.4.4. Внешних ассетов нет: формы, звуки и графика генерируются кодом. pytest для тестов.

## 3. Файлы

| Файл | Ответственность |
|---|---|
| `main.py` | игровой цикл, машина состояний раунда |
| `config.py` | значения по умолчанию |
| `cppn.py` | CPPN: сборка сети из генома, прогон, форма из радиусов |
| `evolution.py` | ГА: турнирный отбор, скрещивание, мутация, элита |
| `track.py` | генерация и эволюция трассы, стены, чекпоинты, проверки геометрии |
| `field.py` | поле расстояний до стен, сетка для лучей и столкновений |
| `car.py` | форма и параметры машинки из генома |
| `sensors.py` | лучи-датчики шагами по полю расстояний |
| `brain.py` | сеть водителя, прогон всей популяции одной матрицей |
| `race.py` | состояние заезда: физика, чекпоинты, приспособленность |
| `train.py` | обучение поколение за поколением, без графики |
| `render.py` | отрисовка трассы, машинок, лучей, камера |
| `ui.py` | боковая панель: кнопки, ползунки, график |
| `sound.py` | синтез звуков |
| `stats.py` | сбор статистики, сохранение и загрузка |

Данные лежат в `saves/`: геномы в `.npz`, история в `stats.json`.

### Поток данных

```
seed -> track.evolve()   -> centerline, walls, checkpoints
                              -> field.build() -> сетка расстояний
seed -> car.generate()   -> silhouette, mass, accel, max_speed, max_steer

evolution.random_population() -> brains (50 x 112)
        |
        v
    race.Race: sensors -> brain -> steer/throttle -> физика -> чекпоинты
        |
        v
    fitness (50,) -> evolution.evolve() -> новые brains
        |
        v
    train.train() отдаёт статистику поколения в main.py
```

## 4. Генератор трассы

CPPN: вход `(sin θ, cos θ)`, три скрытых слоя по 8 нейронов, активации перемешаны (`sin`, гауссиана, `tanh`, `abs`), выход - радиус `r`. Синус и косинус на входе гарантируют замкнутость: при θ=0 и θ=2π вход одинаковый.

θ пробегает 360 значений. `r` нормируется в `[R_MIN, R_MAX]`, точки переводятся в декартовы координаты, кривая сглаживается скользящим средним.

Геном трассы - веса CPPN. Топология 2 -> 8 -> 8 -> 8 -> 1 даёт 24 + 72 + 72 + 9 = 177 чисел.

### Отбор и эволюция

Популяция 200 геномов, 15 поколений. Приспособленность трассы:

- минимальный радиус кривизны больше `TRACK_WIDTH * 0.75` (иначе приспособленность 0). Это главная проверка: если поворот круче, внутренняя стена складывается сама на себя и трасса рвётся
- длина в пределах `[LEN_MIN, LEN_MAX]`, отклонение штрафуется
- самопересечений стен нет, проверяется на прореженной вчетверо линии векторно (иначе 0). Считается только для геномов, прошедших первые две проверки
- разнообразие кривизны: стандартное отклонение кривизны вдоль трассы, это основная награда

Ползунок "сложность" поднимает требуемый порог разнообразия кривизны.

Порядок проверок важен для скорости. Кривизна и длина считаются за O(n) и отсекают почти весь брак, дорогая проверка пересечений доходит до единиц геномов.

### Стены и чекпоинты

Стены - центральная линия, сдвинутая на `±TRACK_WIDTH/2` по нормали. Чекпоинты - поперечные отрезки от стены до стены через каждые `CHECKPOINT_STEP` точек центральной линии, пронумерованы по порядку. Старт и финиш на θ=0.

## 5. Генератор машинки

CPPN проще: вход `(sin φ, cos φ)`, два скрытых слоя по 6, выход - радиус силуэта. Топология 2 -> 6 -> 6 -> 1 даёт 18 + 42 + 7 = 67 чисел. 24 точки по кругу дают многоугольник кузова вид сверху.

Физика выводится из формы, а не задаётся отдельно:

- площадь силуэта -> масса
- отношение длины к ширине -> максимальный угол поворота руля (длинная узкая машина поворачивает хуже)
- масса и длина -> мощность двигателя, отмасштабированная так, чтобы машина оставалась управляемой

Цвет берётся из генома.

## 6. Водитель

Вход - 8 чисел:

- 7 лучей под углами -90, -60, -30, 0, 30, 60, 90 градусов, расстояние до стены, нормированное в `[0, 1]` делением на `RAY_MAX`
- текущая скорость, нормированная на максимальную

Скрытый слой 10 нейронов, `tanh`. Выход 2 числа, `tanh`: руль `[-1, 1]` и газ `[-1, 1]` (отрицательный газ - торможение).

Размер генома: 8*10 + 10 + 10*2 + 2 = 112.

Вся популяция считается одной операцией: `np.einsum` по матрице весов `(50, 8, 10)`.

### Приспособленность

```
fitness = checkpoints_passed * 100
        + progress_in_current_segment * 100
        + (finish_bonus if finished else 0)
```

`progress_in_current_segment` - доля пути от последнего взятого чекпоинта до следующего, число от 0 до 1. Считается как проекция вектора "от чекпоинта до машинки" на вектор "от чекпоинта до следующего чекпоинта", делённая на длину второго и обрезанная в `[0, 1]`. Без этого слагаемого приспособленность растёт ступеньками и отбор плохо различает близкие результаты.

`finish_bonus = (STEPS_PER_GEN - steps_used) * 0.5`, то есть быстрее доехал - больше награда.

Чекпоинт считается взятым, когда машинка пересекла его плоскость: скалярное произведение вектора "от точки чекпоинта до машинки" на направление трассы в этой точке стало неотрицательным. Номер чекпоинта только растёт, вернуться назад нельзя.

Проверка по расстоянию до середины чекпоинта не годится. Машинка, идущая вдоль стены, отстоит от середины на половину ширины трассы и не засчитала бы ни одного чекпоинта, хотя едет правильно. Плюс расстояние между чекпоинтами зависит от длины трассы: при 360 точках и длине 2050 пикселей это 45.6 пикселя, и любой разумный радиус засчитывания оказывается с ним сравним.

Финиш - взяты все чекпоинты, то есть пройден полный круг.

Машинка умирает при касании стены. Дополнительно: если новый чекпоинт не взят за `IDLE_LIMIT` шагов, машинка тоже умирает. Это убирает стратегию "крутиться на месте".

### Генетический алгоритм

Элита `ELITE_FRAC` переходит без изменений. Остальные - потомки: турнирный отбор по 3, равномерное скрещивание (каждый ген с вероятностью 0.5 от одного из родителей), гауссова мутация с силой `MUT_SIGMA` и вероятностью `MUT_RATE` на ген.

`evolution.py` не знает, что за геном ему дали. На вход массив `(N, G)` и массив приспособленностей `(N,)`, на выход новый массив `(N, G)`. Используется и для трасс, и для мозгов.

## 7. Физика

Модель без заносов, чистая кинематика. Состояние популяции: `pos (50, 2)`, `angle (50,)`, `speed (50,)`, `alive (50,)` булев.

Шаг:

```
speed += (throttle * power / mass - speed * DRAG) * dt
speed  = clip(speed, 0, max_speed)
angle += steer * max_steer * (speed / max_speed) * dt
pos   += [cos(angle), sin(angle)] * speed * dt
```

Шаг `dt` фиксированный, `1/60` секунды, и не зависит от реального FPS. Иначе на разных машинах эволюция сходилась бы по-разному и результаты нельзя было бы сравнивать.

Множитель `speed / max_speed` в повороте руля нужен, чтобы стоящая машина не крутилась на месте.

Умножение на `alive` замораживает мёртвые машинки. Ни одного `if` по машинкам, всё маски.

### Поле расстояний

Пересекать луч со всеми отрезками стен нельзя по скорости: 50 машинок × 7 лучей × 720 отрезков = 252 тысячи проверок на шаг, за поколение 450 миллионов.

Вместо этого один раз на трассу строится сетка с шагом 6 пикселей, в каждой клетке лежит расстояние до стены со знаком: положительное внутри полотна, отрицательное снаружи. Так как трасса это труба вокруг центральной линии, значение считается как `TRACK_WIDTH/2 - расстояние до ближайшей точки центральной линии`.

Луч шагает по этому полю: 32 пробы с шагом 8 пикселей, первая проба с отрицательным значением даёт расстояние до стены. Если ни одна не сработала, возвращается `RAY_MAX`. Всё считается сразу для всех машинок массивом формы `(N, 7, 32)`.

Столкновение берётся из того же поля: машинка мертва, если значение в её точке меньше половины ширины кузова. Габариты отдельно считать не нужно.

Построение поля стоит около 0.2 секунды на трассу и делается один раз.

## 8. Цикл игры

Состояния:

- `TRACK_GEN` - эволюция трассы, доли секунды
- `CAR_GEN` - генерация машинки, мгновенно
- `TRAINING` - поколения идут одно за другим, видно на экране
- `SHOWCASE` - показательный заезд лучшего мозга, камера следует за машинкой
- `PAUSED`

Поколение заканчивается, когда все машинки мертвы или прошло `STEPS_PER_GEN` шагов. Обучение заканчивается по `MAX_GENERATIONS` или когда лучший доехал до финиша.

Скорость симуляции: x1, x5, x20, "без отрисовки". Реализуется как N шагов физики за один кадр, рисуется только последний. Режим "без отрисовки" считает поколение целиком за кадр.

## 9. Интерфейс

Окно 1280x720. Слева трасса целиком, масштаб подгоняется под её габариты. Справа панель шириной 300.

Панель сверху вниз:

- поколение, живых из N, лучшая приспособленность, средняя, процент доехавших, лучшее время
- график лучшей и средней приспособленности по поколениям, рисуется линиями
- ползунки: размер популяции, число поколений, сила мутации, доля элиты, ширина трассы, сложность трассы, громкость
- переключатели: скорость симуляции, "с нуля / продолжить с лучшим мозгом", генератор трассы
- кнопки: новый раунд, новая трасса, показательный заезд, сохранить мозг, загрузить мозг

Изменение размера популяции применяется со следующего раунда, остальные ползунки действуют сразу.

Переключатель генератора трассы пока имеет один рабочий пункт, `CPPN + эволюция`. Второй пункт, `обученная модель`, зарезервирован под задачу 19 плана и показывается неактивным, пока файла модели нет. Оба генератора обязаны возвращать один и тот же объект `Track`, чтобы остальная игра не знала, откуда взялась форма.

Мёртвые машинки остаются на экране бледными, видно место массовых аварий. У лидера рисуются лучи-датчики.

## 10. Звук

Всё синтезируется numpy при старте, файлов нет.

- двигатель: 12 зацикленных пилообразных сэмплов на разных частотах, по скорости лидера выбирается нужный и плавно переключается
- удар о стену: всплеск белого шума с экспоненциальным затуханием, около 120 мс
- финиш: три ноты вверх

Звук берётся только от лидера. Если аудиоустройства нет, `pygame.mixer.init()` падает - ловим и работаем молча.

## 11. Сохранение

`saves/brains.npz` - лучшие геномы с их приспособленностью. `saves/stats.json` - всего раундов, трасс пройдено, лучшее время, среднее число поколений до финиша.

Загрузка мозга с несовпадающим размером генома (поменяли число лучей или скрытый слой) отбрасывается с сообщением в панели, обучение начинается с нуля.

## 12. Краевые случаи

- Эволюция трассы не нашла ни одного валидного гена за 15 поколений: смягчаем требования (порог кривизны вниз, пределы длины шире) и пробуем ещё раз, максимум 3 попытки, потом берём круг.
- Все машинки в первом поколении умерли мгновенно и приспособленность у всех нулевая: турнирный отбор вырождается, поэтому при нулевой дисперсии приспособленности популяция генерируется заново случайно.
- Луч не пересёк ни одну стену: возвращаем `RAY_MAX`.
- Ползунок размера популяции меняет форму матриц, поэтому применяется только между раундами.

## 13. Значения по умолчанию

Всё это лежит в `config.py`. Числа в пикселях и в секундах.

| Константа | Значение | Смысл |
|---|---|---|
| `WINDOW_W`, `WINDOW_H` | 1280, 720 | размер окна |
| `PANEL_W` | 300 | ширина боковой панели |
| `FPS` | 60 | целевая частота кадров |
| `DT` | 1/60 | шаг физики, фиксированный |
| `POP_SIZE` | 50 | машинок в поколении |
| `HIDDEN` | 10 | нейронов в скрытом слое водителя |
| `MAX_GENERATIONS` | 60 | предел обучения |
| `STEPS_PER_GEN` | 1800 | шагов на поколение, это 30 секунд |
| `IDLE_LIMIT` | 300 | шагов без нового чекпоинта до смерти |
| `ELITE_FRAC` | 0.1 | доля элиты |
| `MUT_SIGMA` | 0.15 | сила гауссовой мутации |
| `MUT_RATE` | 0.2 | вероятность мутации гена |
| `TOURNAMENT_K` | 3 | размер турнира |
| `RAY_ANGLES` | -90..90, 7 штук | углы датчиков в градусах |
| `RAY_MAX` | 250 | дальность датчика |
| `TRACK_POINTS` | 360 | точек центральной линии |
| `TRACK_WIDTH` | 90 | ширина полотна |
| `CHECKPOINT_STEP` | 8 | через сколько точек ставить чекпоинт |
| `CHECKPOINT_RADIUS` | 45 | радиус засчитывания чекпоинта |
| `FIELD_CELL` | 6 | шаг сетки поля расстояний |
| `RAY_SAMPLES` | 32 | проб вдоль луча |
| `RAY_STEP` | 8 | шаг пробы вдоль луча |
| `R_MIN`, `R_MAX` | 120, 330 | пределы радиуса трассы |
| `LEN_MIN`, `LEN_MAX` | 1800, 4500 | пределы длины трассы |
| `TRACK_POP` | 200 | геномов в эволюции трассы |
| `TRACK_GENS` | 15 | поколений эволюции трассы |
| `SMOOTH_WINDOW` | 15 | окно сглаживания центральной линии |
| `DRAG` | 0.9 | коэффициент трения |
| `CAR_POINTS` | 24 | точек силуэта машинки |
| `ENGINE_LEVELS` | 12 | сэмплов звука двигателя |
| `SAMPLE_RATE` | 22050 | частота дискретизации звука |

Ползунки в панели меняют `POP_SIZE`, `MAX_GENERATIONS`, `MUT_SIGMA`, `ELITE_FRAC`, `TRACK_WIDTH`, порог сложности и громкость. Остальное правится в файле.

## 14. Тесты

pytest, только то, что проверяется без графики:

- трасса замкнута, первая и последняя точка совпадают
- сгенерированная трасса не имеет самопересечений
- чекпоинты идут по порядку вдоль центральной линии
- мутация сохраняет размер генома
- скрещивание даёт гены только от родителей
- мёртвая машинка не меняет позицию после шага физики
- пересечение луча с известным отрезком даёт правильное расстояние
- приспособленность растёт при взятии чекпоинта

Графика и звук проверяются глазами.

## 15. Документация

Под каждый модуль пишется отдельный документ в `docs/modules/`, одноимённый с файлом:

`docs/modules/config.md`, `cppn.md`, `evolution.md`, `track.md`, `field.md`, `car.md`, `sensors.md`, `brain.md`, `race.md`, `train.md`, `render.md`, `ui.md`, `sound.md`, `stats.md`, `main.md`.

Каждый документ содержит:

- зачем нужен модуль и что он не делает
- математика, стоящая за кодом, с формулами
- разбор публичных функций: параметры, что возвращает, формы массивов
- пример вызова
- разбор неочевидных решений и почему сделано именно так
- с какими модулями связан

Плюс `README.md` в корне: что это, как запустить, управление, структура проекта, ссылки на документы модулей.

Документация обновляется вместе с кодом, а не в конце.

## 16. Репозиторий

Git, remote `origin` - Gitea в локальной сети.

Автор коммитов берётся из настроек репозитория (`git config user.name` / `user.email`).

Коммит после каждого законченного куска: спецификация, план, затем каждый модуль вместе со своей документацией и тестами.

`.gitignore`: `__pycache__/`, `*.pyc`, `saves/`, `.pytest_cache/`, `.remember/`.

## 17. Что не делаем

- обучение с подкреплением (PPO, DQN)
- NEAT с эволюцией топологии
- 3D, спрайты, текстуры
- заносы, шины, аэродинамика
- многопользовательский режим, соревнование мозгов между собой
- обучение генератора трассы на реальных трассах
