# Проверка на баги

Дата: 2026-09-05. Задача 18 плана.

Проверялось четырьмя способами: краевые случаи из спецификации, крайние положения всех ползунков, целенаправленный поиск дыр в логике и профилирование.

## Что проверено и работает

### Крайние положения ползунков

Каждая настройка ставилась в оба конца шкалы, после чего запускался полный раунд.

| Настройка | Крайние значения | Результат |
|---|---|---|
| популяция | 10 и 120 | без падений |
| поколений | 5 и 120 | без падений |
| мутация | 0.01 и 0.6 | без падений |
| элита | 0.0 и 0.4 | без падений |
| ширина трассы | 25 и 80 | без падений |
| сложность | 0.0 и 1.0 | без падений |
| громкость | 0.0 и 1.0 | без падений |

Проверено и то, что ограничения действительно соблюдаются: при ширине 80 и лёгкой сложности минимальный радиус вышел 116 при требуемых 108, при ширине 25 и высокой сложности 30 при требуемых 18.75.

### Краевые случаи из спецификации

| Случай | Ожидание | Факт |
|---|---|---|
| все оценки нулевые | популяция пересоздаётся | пересоздана, разброс 1.0 |
| невыполнимые требования к трассе | возвращается круг | круг |
| битый файл мозгов | загрузка вернёт `None` | `None` |
| битый `stats.json` | пустые счётчики | пустые |
| луч вне сетки поля | минимальное расстояние | минимальное |
| смена размера популяции на ходу | применяется со следующего раунда | текущий заезд 50, следующий 17 |

### Целенаправленный поиск: можно ли взять чекпоинт даром

Была реальная причина для беспокойства. Чекпоинт засчитывается пересечением его плоскости, а плоскость бесконечна. При минимальном радиусе 34 и шаге чекпоинтов 45 пикселей направление трассы между соседними чекпоинтами разворачивается на 76 градусов. Значит в крутом повороте машинка могла бы оказаться "впереди" сразу нескольких плоскостей и получить чекпоинты, не проехав их.

Проверка: машинка ставится точно на каждый чекпоинт каждой из четырёх трасс и 60 раз подряд вызывается засчитывание без единого движения.

Результат: **ноль даровых чекпоинтов** на всех трассах и во всех точках. Опасение не подтвердилось, потому что 76 градусов это разворот в самой крутой точке трассы, а не типичный, и сглаживание убирает локальные всплески.

## Что найдено и исправлено

### Сохранение после показательного заезда не читалось

Кнопка "сохранить", нажатая после конца раунда, писала 50 геномов и одну оценку. Приспособленность бралась у текущего заезда, а в показательном заезде участвует одна машинка. Загрузка справедливо отбраковывала такой файл как несогласованный, и игрок видел "сохранения нет" сразу после успешного сохранения.

Исправлено: игра хранит приспособленность последнего обученного поколения отдельно от текущего заезда.

### Кадр упирался в отрисовку машинок

Профилирование при 50 машинках на скорости x1:

| Что | Было |
|---|---|
| шаг физики | 0.41 мс |
| отрисовка трассы | 2.32 мс |
| **отрисовка машинок** | **6.42 мс** |
| панель | 0.88 мс |
| полный кадр | 14.0 мс, то есть 70 кадров/с |

70 кадров в секунду при цели 60 означает отсутствие запаса: любая просадка сразу видна.

Причина не в pygame, а в подготовке данных. На каждую машинку приходилось шесть отдельных преобразований координат (четыре колеса, кузов, стекло) и шесть питоновских циклов по точкам с распаковкой кортежей.

Исправлено двумя правками:

- все детали машинки собраны в один массив `Car.stacked` с таблицей срезов, поэтому преобразование координат делается один раз на машинку вместо шести
- перевод в целые координаты идёт через `astype(np.int32).tolist()` вместо генератора списка с распаковкой

| Что | Стало |
|---|---|
| отрисовка машинок | 2.49 мс |
| полный кадр | 5.59 мс, то есть 179 кадров/с |

Втрое быстрее, запас втрое от нужных 60 кадров. Картинка при этом не изменилась, что проверено сравнением скриншотов.

## Ошибки, найденные раньше по ходу работы

Они уже исправлены и разобраны в документах модулей, здесь только перечень.

| Что | Как нашлось | Где разобрано |
|---|---|---|
| радиус поворота считался вчетверо больше истинного | сверкой формулы кривизны на круге | `track.md` |
| отбор предпочитал бракованные трассы годным | тестом на порядок оценок | `track.md` |
| ползунок сложности ничего не делал | замером по сидам | `track.md` |
| метрика разнообразия предпочитала круг трилистнику | картинкой и замером | `track.md` |
| активация `abs` давала дефект на линии старта | картинкой | `cppn.md` |
| разброс весов CPPN насыщал выход на 54 процента | замером | `cppn.md` |
| перебор настроек не работал: значения по умолчанию из конфига берутся при импорте | четыре одинаковых результата подряд | `cppn.md` |
| масса машинки упиралась в потолок у всех | замером распределения | `car.md` |
| вытянутость машинок почти отсутствовала | замером распределения | `car.md` |
| засчитывание чекпоинта по расстоянию не работало для машинки вдоль стены | арифметикой при написании кода | `race.md` |
| камера отпрыгивала после показательного заезда | скриншотом | `main.md` |
| финишировавшая машинка красилась как разбившаяся | скриншотом | `main.md` |
| строка "время" налезала на блок трассы | скриншотом | `main.md` |
| `any` с генератором обрывал рассылку событий по виджетам | разбором кода | `ui.md` |

Половина найдена картинками и замерами, а не тестами. Тесты хорошо стерегут то, что уже понято, но плохо ловят "работает, но не то".

## Состояние на конец проверки

203 теста, полторы минуты. Падений нет.
