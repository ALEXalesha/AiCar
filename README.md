<div align="center">

# AI Car Racing

**A sandbox where everything is generated and everything is learned: one network draws the track, a second draws the car, and fifty more learn to drive it by evolution, live on screen.**

[Download for Windows](https://github.com/ALEXalesha/AiCar/releases/latest) &nbsp;·&nbsp; [Русская версия этого файла](README.ru.md)

[![CI](https://github.com/ALEXalesha/AiCar/actions/workflows/ci.yml/badge.svg)](https://github.com/ALEXalesha/AiCar/actions/workflows/ci.yml)
[![Release](https://img.shields.io/github/v/release/ALEXalesha/AiCar?color=16a34a)](https://github.com/ALEXalesha/AiCar/releases/latest)
[![License](https://img.shields.io/badge/license-MIT-blue)](LICENSE)

<img src="docs/preview_game.png" width="900" alt="Training: 41 of 50 cars still alive">

</div>

> **The interface is in Russian**, and so is the long write-up - [README.ru.md](README.ru.md), which this file summarises. In the screenshot, "обучение" means training, "живых 41/50" is how many cars are still alive, and "поколение 4" is the generation number.

## Three networks, three different jobs

**1. A network draws the track.** There is no dataset of racing tracks, and building one is a project of its own. So the track comes from a CPPN - a pattern-producing network whose weights are never trained, only sampled. Because sines, gaussians and wavelets sit inside it, random weights give organic shapes rather than noise.

The network takes an angle θ and returns a radius r(θ). The detail that makes it work: the input is not the angle itself but its harmonics `sin θ, cos θ, sin 2θ … cos 3θ`. All of them have period 2π, so θ=0 and θ=2π give the same input, the same radius, and a loop that is closed by construction. Feed the angle directly and the ends do not meet.

Those 209 weights *are* the track's genome, so they can be crossed and mutated: 200 candidates, 15 generations, rewarded for varied shape and punished for impassable corners.

**A second generator is a properly trained model.** Any track is 360 radii - a fixed-length vector, which is easy to learn from. The dataset was generated by the CPPN itself: 400 independent evolution runs, 5000 tracks. A VAE compresses the profile into 12 numbers and reconstructs it, and generating is then just a random point in that 12-dimensional space.

| Generator | Interest (median) | Time per track |
| --- | --- | --- |
| CPPN + evolution | 59.3 | 2243 ms |
| Trained model | 59.9 | 48 ms |

Same quality, 47× faster: the model does not search for a shape, it remembers it. Training uses PyTorch in a separate script, but **playing needs no torch at all** - the decoder's weights are a 498 KB file in the repository, and the decoder itself is three matrix multiplications in numpy.

**2. A network draws the car**, moulding a body over a rounded frame, and the physics is derived from the shape rather than rolled separately: silhouette area gives mass, elongation gives top speed and steering sharpness. A long narrow car is faster and worse in corners.

**3. A network drives.** The car sees the world through seven range sensors from −90° to +90°, plus its own speed - eight numbers in, `8 → 10 (tanh) → 2` (steering and throttle), 112 weights. It knows nothing else: not where it is, not where the finish is.

There are no correct answers to imitate, only a score at the end of a run, so it learns by a genetic algorithm: fifty cars drive at once, whoever got further leaves offspring, the top 10 % carry over unchanged so a good solution is not lost to a bad mutation.

**Fitness is the most fragile part of the whole project** - far more fragile than the network. Distance travelled would teach the cars to spin in place; straight-line distance from the start would park them in the first corner. Only checkpoints, placed every 45 pixels along the track, count movement in the right direction. It is exactly the reward hacking seen in large models: the agent optimises the metric you wrote, not the one you meant.

## In numbers

Across twelve random seeds on the `сложный` (hard) level: ten of twelve finish, median 16.5 generations, and not one random brain completes a lap straight away. One generation without graphics takes 0.27 s.

The learning curve is almost always the same shape: a long plateau, then a jump. That is how evolution works - it does not improve a solution gradually, it waits for the lucky mutation that opens the next piece of track.

## Why it runs fast

Fifty cars are not fifty objects but arrays of shape `(50, …)`. Physics, sensors and the network forward pass are computed for the whole generation at once, with no loop over cars and no `if` in the physics at all - dead cars are frozen by multiplying with a boolean mask.

The sensors deserve their own note. Done naively that is 50 cars × 7 rays × 720 wall segments - 252 000 intersection tests per physics step, 450 million per generation. Instead a distance field of the track is built once, and a ray does not intersect anything: it steps along the grid and reads values. The same grid gives collision detection for free.

## Running it

```bash
pip install numpy pygame
python main.py
```

Training without graphics, in the console:

```bash
python train.py --seed 3 --generations 60
```

Ready Windows builds - installer and portable - are on the [releases page](https://github.com/ALEXalesha/AiCar/releases/latest); Python, numpy and pygame are inside, nothing else is needed.

## Tests

```bash
python -m pytest
```

278 tests, about two minutes - everything that can be checked without a window: track geometry, the distance field, sensors, physics, checkpoints, fitness, the genetic algorithm, widget logic, sound synthesis, saves.

Beside them is a sweep of invariants over random data:

```bash
python stress.py --rounds 10
```

98 properties that must hold for any input, each run hundreds of times on random cases - 8851 checks per round. Ten rounds take about thirteen minutes and one just over a minute; CI runs one round together with the tests after every push, and the script exits non-zero on any violation. Three of them drive the real game for hundreds of frames, pressing keys at random, and verify the invariants after every frame. One is checked pixel by pixel: the telemetry box is drawn over a flat background, and afterwards nothing may be painted outside its bounds - so it catches any overflow rather than the one the author had in mind.

Two sweeps, 127 360 checks, two findings. One property was simply formulated wrong: `the contour is closed` in fact compared the step at the seam against an ordinary step and called a sixfold excess a violation - that is a smoothness check with a threshold pulled out of thin air. The other finding was a real bug: the summary line ran off the panel at six-digit counters, because the test guarding its width counted characters, and the font is proportional.

**Where this stops working.** The four bugs found last were found by playing and looking at the screen. The line did not overflow - it was truncated. The text did not leave the panel - it was painted over. The leader really was the best car; it had just crashed, and the camera stayed on the wreck while two cars were still driving. The round counter really did only grow - by one extra round every time `load` was pressed. Every contract was honoured and the result was still wrong. A property guards a contract; it does not catch "the code honours the contract, but the contract is not the right one". All of it is written up in [docs/bug-hunt.md](docs/bug-hunt.md).

## Preview frames are generated

`tools/make_previews.py` runs the game with no window - the same way its own self-check does: SDL's dummy driver renders into an in-memory surface, and the frame is taken straight from it.

```bash
python tools\make_previews.py
```

A screen grab would be wrong twice over. A window that just opened can sit behind others, and then the shot catches someone else's content. More importantly, the moment cannot be caught by hand: the frame where part of the generation already lies grey in one corner lasts a fraction of a second. The script looks for it by the state of the race rather than by frame number, and with a fixed seed it reproduces run after run.

## Stack

Python · NumPy · pygame · PyTorch (only to train the track VAE) · PyInstaller · NSIS

## Licence

MIT, see [LICENSE](LICENSE).
